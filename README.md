# Clinic Queue Manager

This is a simple Flask web app that allows users to add patients and view a waiting list.

Features:
- Add patients
- View queue (FIFO)
- Timestamp for each patient

How to run:

1. Install Flask
pip install flask

2. Run app
python app.py

3. Open browser
http://127.0.0.1:5000
